import * as Slider from '@radix-ui/react-slider';

export const sliderRoot = Slider.Root;
export const sliderTrack = Slider.Track;
export const sliderRange = Slider.Range;
export const sliderThumb = Slider
